{
  "name": "hawk.desktop",
  "version": "0.0.1",
  "description": "Desktop version of hawk.garage",
  "license": "UNLICENSED",
  "main": "./src/main.js",
  "scripts": {
    "start": "electron .",
    "submodule:build": "cd hawk.garage && git fetch && git checkout vue-config-addition && yarn install && yarn build",
    "submodule:update": "git submodule foreach git pull origin vue-config-addition",
    "submodule:prepare": "npm run submodule:update && npm run submodule:build"
  },
  "devDependencies": {
    "electron": "^4.1.4"
  }
}
