{
  "name": "hawk.desktop",
  "version": "0.0.1",
  "description": "Desktop version of hawk.garage",
  "license": "UNLICENSED",
  "main": "./src/main.js",
  "scripts": {
    "start": "electron .",
    "update": "git submodule foreach git pull origin master && cd hawk.garage && yarn install && yarn build"
  },
  "devDependencies": {
    "electron": "^4.1.4"
  }
}
